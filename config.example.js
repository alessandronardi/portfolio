// File di configurazione di esempio.
// Copia questo file in 'config.js' e inserisci le tue credenziali se desideri abilitare le chiamate dirette.
// Nota: config.js non deve essere tracciato su repository pubbliche.

const SECRET_CONFIG = {
    // Opzionale: Chiave Google Gemini API per il chatbot interattivo
    GEMINI_API_KEY: '',
    
    // Opzionale: Webhook per tracciamento eventi o routing custom
    N8N_WEBHOOK_URL: ''
};
